---
interface Props {
	clients: string[];
}

const { clients } = Astro.props;
---

<div class="flex items-center gap-12 px-4">
	{
		clients.map((client) => (
			<div class="whitespace-nowrap rounded-md bg-secondary-400 p-3 text-lg font-medium text-gray-900">
				{client}
			</div>
		))
	}
</div>
