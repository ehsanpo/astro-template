---
import { getPortfolioData } from "../../utils/data";
import Heading from "../ui/heading/Heading";
import SectionTitle from "../ui/SectionTitle.astro";

const { awards } = await getPortfolioData();
---

<section class="py-20 bg-white dark:bg-neutral-800">
  <div class="container mx-auto px-4">
    <Heading level="h3" gradient floating backplate client:idle>
      Awards
    </Heading>

    <div
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 text-center"
    >
      {
        awards.map((award) => (
          <a
            href={award.link}
            class="group clip bg-opium  dark:bg-neutral-700 p-6 rounded-lg transition-transform hover:-translate-y-1"
          >
            <div class="">
              <img
                src={award.images}
                alt={award.name}
                class="mx-auto rounded-lg mb-4 group-hover:scale-105 transition-transform"
              />
              <h3 class="font-semibold text-xl  text-yellow-500 mb-2">
                {award.name}
              </h3>
              <p class="dark:text-gray-400">{award.description}</p>
            </div>
          </a>
        ))
      }
    </div>
  </div>
</section>
